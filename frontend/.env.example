# Optional. The frontend calls the backend at http://localhost:5000 by default.
# To point at a different backend, set this and read it in code via import.meta.env.
VITE_API_BASE_URL=http://localhost:5000
