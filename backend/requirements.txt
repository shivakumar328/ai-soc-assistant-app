# Core backend dependencies — this is all you need to RUN the SOC Assistant API.
Flask==3.1.3
Flask-CORS==6.0.5
python-dotenv==1.2.2
anthropic==0.112.0
openai==1.51.0
requests==2.32.3

# For the optional advanced_integrations.py connectors, also install:
#   pip install -r requirements-full.txt
